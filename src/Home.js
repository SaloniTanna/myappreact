import React from "react";
import { render } from '@testing-library/react';

class Home extends React.Component{
    render(){
        return(
        <div>
            <h1>You have clicked on Home</h1>
        </div>
        )
    }
}

export default Home;